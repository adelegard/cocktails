-@pageTitle = "Upload Photo for - #{@recipe.title}"

this is currently broken
=form_for @recipe_user, :html => { :multipart => true } do |f|
	= hidden_field(:recipe_user, :recipe_id)
	= f.file_field :photo
	= f.submit